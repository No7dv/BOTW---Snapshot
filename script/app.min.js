const ratio = .1, options = { root: null, rootMargin: "0px", threshold: .1 }, handleIntersect = function (e, t) { e.forEach(function (e) { e.intersectionRatio > .1 && (e.target.classList.add("reveal-visible"), t.unobserve(e.target)) }) }, observer = new IntersectionObserver(handleIntersect, options); document.querySelectorAll(".reveal").forEach(function (e) { observer.observe(e) }); const video = document.getElementById("myVideo"); video.addEventListener("loadedmetadata", () => { video.classList.add("fade-in") }), video.addEventListener("timeupdate", () => { let e = video.duration - video.currentTime; e <= 1 && !video.classList.contains("fade-out") && video.classList.add("fade-out") }), video.addEventListener("ended", () => { video.classList.remove("fade-out"), video.currentTime = 0, video.play() }), window.addEventListener("DOMContentLoaded", () => { let e = document.querySelector(".carrousel-track"), t = Array.from(e.children); t.forEach(t => { let o = t.cloneNode(!0); e.appendChild(o) }); let o = .3, d = 0; function n() { Math.abs(d -= o) >= e.scrollWidth / 2 && (d = 0), e.style.transform = `translateX(${d}px)`, requestAnimationFrame(n) } n() });